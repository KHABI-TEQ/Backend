/**
 * Request To Market: the amount payable by the Publisher to the Agent is now taken from the
 * property's stored agentCommissionAmount (set by Landlord/Developer when creating the property).
 * This constant is no longer used when creating requests; kept for reference only.
 */
export const REQUEST_TO_MARKET_FEE_NAIRA_LEGACY = 50_000;
